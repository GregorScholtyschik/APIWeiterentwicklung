components:
  schemas:
    switchingTimeState:
      type: string
      description: "Schalthandlung an einer Lokation und der darausfolgenden Status"
      x-Translation:
        de: "Schalthandlung"
      example: "powerOn"
      enum:
      - powerOn
      - powerOff