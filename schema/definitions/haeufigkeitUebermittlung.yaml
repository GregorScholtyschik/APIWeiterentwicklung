components:
  schemas:
    haeufigkeitUebermittlung:
      type: string
      x-Translation:
        de: "Häufigkeit der Übermittlung"
      description: "Häufigkeit der Übermittlung"
      example: "einmalig"
      enum:
      - jaehrlich
      - einmalig