components:
  schemas:
    reasonNegativeControlMeasures:
      type: string
      description: "Grund"
      example: "communicationFailure"
      enum:
      - communicationFailure
      - overload
      - unable