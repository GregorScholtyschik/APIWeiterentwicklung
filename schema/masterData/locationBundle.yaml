components:
  schemas:
    locationBundle:
      required:
      - identificationDateTime
      type: object
      x-Translation:
        de: "Lokationsbündel"
      properties:
        identificationDateTime:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'
          description: "Zeitpunkt zu dem die Identifikation stattfinden soll"
