components:
  schemas:
      uniqueRolloutSwitchingTimeStates:
        type: array
        minItems: 1
        description: "Array zur Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen Status der Schalthandlung."
        x-Translation:
          de: "Array zur Angabe Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen Status der Schalthandlung."
        items:
          $ref: './uniqueRolloutSwitchingTimeState.yaml#/components/schemas/uniqueRolloutSwitchingTimeState'