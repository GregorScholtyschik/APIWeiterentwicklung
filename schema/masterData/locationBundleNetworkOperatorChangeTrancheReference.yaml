components:
  schemas:
    locationBundleNetworkOperatorChangeTrancheReference:
      required:
      - regardTrancheId
      - supplier
      type: object
      x-Translation:
        de: "Eine Tranche mit ihren zugehörigen Marktpartnern"
      properties:
        regardTrancheId:
          $ref: './../identifier/trancheId.yaml#/components/schemas/trancheId'
          description: "Identifiziert die Tranche mittels einer eindeutigen ID"
        supplier:
          $ref: './../identifier/marketPartnerId.yaml#/components/schemas/marketPartnerId'
          description: "Lieferant, der der Tranche zugeordnet ist"