components:
  schemas:
    locationBundleNetworkOperatorChangeMeloReference:
      required:
      - regardNeloId
      - measuringPointOperators
      type: object
      x-Translation:
        de: "Eine Netzlokation mit ihren zugehörigen Marktpartnern"
      properties:
        regardNeloId:
          $ref: './../identifier/neloId.yaml#/components/schemas/neloId'
          description: "Identifiziert die Netzlokation mittels einer eindeutigen ID"
        measuringPointOperators:
          $ref: './../identifier/marketPartnerId.yaml#/components/schemas/marketPartnerId'
          description: "Messstellenbetreiber, der der Netzlokation zugeordnet ist"