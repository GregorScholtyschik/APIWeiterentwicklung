components:
  schemas:
    result:
      description: "Hier wird bei einer Antwort der Entscheidungsbaum und der entsprechende Antwortcode aus dem Entscheidungsbaumdiagramm angegeben. Wenn sich die Marktlokation nicht mehr im Netzgebiet befindet ist die MP-ID des NB als \"networkOperator\" anzugeben, an den die Marktlokation abgegeben wurde."
      x-Translation:
        de: "Ergebnis"
      required:
      - responseCode
      - decisionTree
      type: object
      properties:
        decisionTree:
          $ref: './decisionTree.yaml#/components/schemas/decisionTree'
        responseCode:
          $ref: './responseCode.yaml#/components/schemas/responseCode'
        reason:
          $ref: './../freeText.yaml#/components/schemas/freeText'
        identifierNetworkOperator:
          $ref: './../identifier/marketPartnerId.yaml#/components/schemas/marketPartnerId'