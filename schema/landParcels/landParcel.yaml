components:
  schemas:
    landParcel:
      required:
      - districtName
      - lotNumber
      - subLotNumber
      type: object
      x-Translation:
        de: "Grundstück"
      properties:
        districtName:
          $ref: './districtName.yaml#/components/schemas/districtName'
        lotNumber:
          $ref: './lotNumber.yaml#/components/schemas/lotNumber'
        subLotNumber:
          $ref: './subLotNumber.yaml#/components/schemas/subLotNumber'