components:
  schemas:
    tranche:
      required:
      - trancheSuppliers
      - proportion
      - identifierTranche
      type: object
      x-Translation:
        de: "Tranche"
      properties:
        identifierTranche:
          $ref: './../identifier/maloId.yaml#/components/schemas/maloId'
        proportion:
          $ref: './proportionTranche.yaml#/components/schemas/proportionTranche'
        percentLessHundred:
          $ref: './../percentLessHundred.yaml#/components/schemas/percentLessHundred'
        trancheSuppliers:
          $ref: './trancheSuppliers.yaml#/components/schemas/trancheSuppliers'