components:
  schemas:
    marketLocationNetworkOperator:
      required:
      - executionTimeFrom
      - identifierNetworkOperator
      type: object
      x-Translation:
        de: "Netzbetreiber an der Marktlokation"
      properties:
        identifierNetworkOperator:
          $ref: './../identifier/marketPartnerId.yaml#/components/schemas/marketPartnerId'
        executionTimeFrom:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'
        executionTimeUntil:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'

